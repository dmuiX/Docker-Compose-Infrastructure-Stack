# This file is auto-generated by openmediavault (https://www.openmediavault.org)
# WARNING: Do not edit this file, your changes will get lost.

# arcane
# 

services:
  arcane:
    image: ghcr.io/getarcaneapp/arcane:latest
    container_name: arcane
    environment:
      APP_URL: ${APP_URL}
      TZ: ${TZ}
      PUID: ${PUID}
      PGID: ${PGID}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      JWT_SECRET: ${JWT_SECRET}
      DOCKER_HOST: tcp://dockerproxy:2375
      ANALYTICS_ENABLE: false
      ACCENT_COLOR: #00c300
      GLASS_EFFECT_ENABLED: true
      ENABLE_GRAVATAR: true
      AUTO_UPDATE: true
      AGENT_MODE: false
      PRUNE_MODE: all
      PROJECTS_DIRECTORY: /app/data/projects
      OIDC_ADMIN_VALUE: admin_users
      OIDC_ADMIN_CLAIM: groups
      OIDC_MERGE_ACCOUNTS: true
      OIDC_AUTO_REDIRECT_TO_PROVIDER: true
    env_file: arcane.env
    volumes:
      - ./data:/app/data
      - /home/nasadmin/DockerCompose:/app/data/projects
    networks:
      dockerproxy:
      swag:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3552"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    labels:
      swag: enable
      swag.uptime-kuma.enabled: true
      swag.uptime-kuma.monitor.name: arcane
      swag.uptime-kuma.monitor.url: http://arcane:3552

networks:
  dockerproxy:
    external: true
  swag:
    external: true
